# State

```
updated_at: <ISO-8601 UTC timestamp>
```

<!-- One current snapshot of what exists and what is in progress (target 5-15
     lines). Replaced in place on every write, never appended to. -->

## Constraints

<!-- Known limitations and hard requirements, one bullet each. -->
