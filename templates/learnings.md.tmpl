# Learnings

<!-- Append-only. One "## Learning: <short title>" heading per entry, followed
     by a fenced metadata line holding `captured_at` (ISO-8601 UTC) and
     `category: learning`, then the body: the discovery, worth not
     re-discovering. No entries are seeded here — a heading in this template
     would be read back as real project memory. -->

## Conventions

<!-- Established conventions, as "## Convention: <short title>" entries with
     `category: convention`. -->
