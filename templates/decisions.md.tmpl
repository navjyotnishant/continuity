# Decisions

<!-- Append-only. One "## Decision: <short title>" heading per entry, followed
     by a fenced metadata line holding `captured_at` (ISO-8601 UTC) and
     `category: decision`, then the body: what was decided and why, enough that
     it is not re-litigated. No entries are seeded here — a heading in this
     template would be read back as real project memory. -->
