# Tasks

<!-- One "## Task: <short title>" heading per entry, followed by a fenced
     metadata line holding `status` (active | blocked | done), `captured_at`
     and `updated_at` (ISO-8601 UTC) and `category: task`, then the body: what
     the task is, and if blocked, what it is blocked on. Status transitions in
     place; a task is never deleted, only marked done. No entries are seeded
     here — a heading in this template would be read back as real project
     memory. -->
